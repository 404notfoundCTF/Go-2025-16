在工作量证明（ProofofWork，PoW）共识机制中，区块哈希值的计算必须满足网络设定的难度要求。难度
通过一个目标值(target)定义，目标值越小，所需的哈希值前缀零的数量就越多，符合目标值的哈希才能被
认为是有效的。因此，对区块哈希值进行筛选可以帮助验证区块的合法性。请完善SortPoWHash0方法代
码，给定一个难度目标(target)和一个区块哈希值，要求找出所有满足条件的哈希值，即哈希值的前缀零数
量大于或等于目标值。请编写代码判断所给的哈希值是否有效，输出类型为：bool，例如：true。