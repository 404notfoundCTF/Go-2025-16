在区块链结构中，每个区块不仅包含其自身的哈希值，还记录了前一个区块的哈希值，从而形成一个链式结
构。这种结构确保了区块的顺序和数据的完整性，任何区块的篡改都会破坏链条的连接顺序。因此，正确维
护区块的顺序是保障区块链数据有效性的关键。请完善SortBlocks0方法代码，给定一组区块数据，每个区
块包含自身的哈希值和前一个区块的哈希值（假设第一个区块记录的前一个区块的哈希值为“null”），要求根据
这些信息按前后关系构建出正确的区块链顺序。请编写代码遍历区块数据，最终返回区块的顺序数组，输出
格式形如：[]string{“Block1”, “Block2”, “Block3”}。
日
(参考以下区块的结构体数据，包含区块名、自身哈希值以及前一个区块的哈希值
type Block struct {
BlockName string
Hash string
PrevHash string
})